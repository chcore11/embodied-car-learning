# Data

This directory is for simple v0.1 run artifacts.

Expected future outputs:

- CSV logs for simulation runs.
- Small map files if needed.
- Simple metrics derived from runs.

Large datasets, model checkpoints, ROS2 bags, hardware logs, and complex experiment artifacts do not belong in the current v0.1 stage.
